AOS.init({
  duration: 1200, // Zeit in Millisekunden (hier: 1,2 Sekunden)
  once: false // Animationen werden jedes Mal ausgeloest, wenn das Element gescrollt wird
});
console.log('AOS initialisiert!','event', event);
